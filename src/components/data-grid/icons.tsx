import type {MRT_Icons} from 'material-react-table'

type IconProps = Record<string, unknown>
export const dataGridIcons: Partial<MRT_Icons> = {
  // SearchIcon: (props: IconProps) => <SearchNormal1 size={20} {...props} />,
  // FullscreenIcon: (props: IconProps) => (
  //   <Box component={Scan} height={20} width={20} {...props} />
  // ),
}
